import { Rect } from 'dut';
import { expect } from 'chai';

describe('Rect', () => {
  describe('constructor', () => {
    it.skip('constructor', () => {
      expect(true).to.be.false;
    });
  });
});
