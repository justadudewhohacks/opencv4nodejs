const { Rect } = global.dut;
const { expect } = require('chai');

describe('Rect', () => {
  describe('constructor', () => {
    it.skip('constructor', () => {
      expect(true).to.be.false;
    });
  });
});
