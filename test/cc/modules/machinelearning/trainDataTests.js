import cv from 'dut';
import { expect } from 'chai';

module.exports = () => {
  describe('TrainData', () => {
    describe('constructor', () => {
      it('should be constructable with default args', () => {

      });
    });
  });
};
