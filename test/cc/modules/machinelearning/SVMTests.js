import cv from 'dut';
import { expect } from 'chai';

module.exports = () => {
  describe('SVM', () => {
    describe('constructor', () => {
      it('should be constructable with default args', () => {

      });
    });
  });
};
