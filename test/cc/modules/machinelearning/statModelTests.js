import cv from 'dut';
import { expect } from 'chai';

module.exports = () => {
  describe('StatModel', () => {
    describe('constructor', () => {
      it('should be constructable with default args', () => {

      });
    });
  });
};
