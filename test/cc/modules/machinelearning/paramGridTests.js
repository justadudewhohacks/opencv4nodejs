import cv from 'dut';
import { expect } from 'chai';

module.exports = () => {
  describe('ParamGrid', () => {
    describe('constructor', () => {
      it('should be constructable with default args', () => {

      });
    });
  });
};
