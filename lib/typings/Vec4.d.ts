import { Vec } from './Vec.d';

export class Vec4 extends Vec {
  readonly x: number;
  readonly y: number;
  readonly z: number;
  readonly w: number;
  constructor(x: number, y: number, z: number, w: number);
}
